components:
  # Query parameter 정의 모음
  parameters:
    getProductschk_year_Query:
      name: year
      in: query
      description: "조회할 연도"
      required: true
      schema:
        type: string
        example: "2023"
    getProductschk_gubun_Query:
      name: gubun
      in: query
      description: "구분 (0: 전체, 1: 개별)"
      required: true
      schema:
        type: string
        example: "1"

  # Response 정의 모음
  schemas:
    getProducts_Res:
      type: object
      properties:
        success:
          description: 'api 처리결과'
          type: boolean
          example: true
        data:
          description: '계획종합 데이터 Array'
          type: array
          items:
            type: object
            properties:
              GUBUN:
                description: '구분'
                type: string
                example: '1'
              YEAR:
                description: '연도'
                type: string
                example: '2023'
              MONTH:
                description: '월'
                type: string
                example: '01'
              CLASS_CODE:
                description: '분류코드'
                type: string
                example: '01'
              CLASS_NAME:
                description: '분류명'
                type: string
                example: '제품'
              AMOUNT:
                description: '금액'
                type: number
                example: 100000
              RATE:
                description: '비율'
                type: number
                example: 10.5 