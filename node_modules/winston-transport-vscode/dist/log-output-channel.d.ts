import { Config } from 'triple-beam';
import Transport, { TransportStreamOptions } from 'winston-transport';
import type { TransformableInfo } from 'logform';
import type { LogOutputChannel } from 'vscode';
export declare class LogOutputChannelTransport extends Transport {
    private outputChannel;
    constructor(opts: LogOutputChannelTransport.Options);
    log(info: TransformableInfo, next: () => void): void;
}
export declare namespace LogOutputChannelTransport {
    type Options = TransportStreamOptions & {
        outputChannel: LogOutputChannel;
    };
    const config: Config;
    const format: import("logform").FormatWrap;
}
//# sourceMappingURL=log-output-channel.d.ts.map